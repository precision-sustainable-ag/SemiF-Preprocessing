name: semif_preprocessing
channels:
  - conda-forge
  - defaults
dependencies:
  - python==3.11
  - pip
  - hydra-core
  - omegaconf
  - opencv  # Use 'opencv' instead of 'opencv-python'
  - pandas
  - tqdm